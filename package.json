{
  "name": "stripe-onboarding-schema",
  "version": "1.0.0",
  "description": "Schema definition that maps Stripe requirements to a UI schema",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neelav/stripe-onboarding-schema.git"
  },
  "author": "neelav",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/neelav/stripe-onboarding-schema/issues"
  },
  "homepage": "https://github.com/neelav/stripe-onboarding-schema#readme",
  "peerDependencies": {
    "stripe": "^8.56.0"
  },
  "devDependencies": {
    "stripe": "^8.56.0"
  }
}
