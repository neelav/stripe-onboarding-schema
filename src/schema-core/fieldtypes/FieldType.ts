enum FieldType {
    TEXT = 'TEXT',
    BOOLEAN = 'BOOLEAN',
    DOCUMENT = 'DOCUMENT',
    NUMBER = 'NUMBER',
    ENUM = 'ENUM',
    ADDRESS = 'ADDRESS',
    UNKNOWN = 'UNKNOWN'
}

export default FieldType;
