export enum RequirementsType {
    PAST_DUE = 'past_due',
    CURRENTLY_DUE = 'currently_due',
    EVENTUALLY_DUE = 'eventually_due',
}

export enum Country {
    US = 'US',
    CA = 'CA',
    GB = 'GB',
    FR = 'FR',
}
